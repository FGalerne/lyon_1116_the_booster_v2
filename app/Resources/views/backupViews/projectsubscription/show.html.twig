{% extends 'base.html.twig' %}

{% block body %}
    <h1>Projectsubscription</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projectSubscription.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ projectSubscription.message }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ projectSubscription.status }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('projectsubscription_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('projectsubscription_edit', { 'id': projectSubscription.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
